# Set Up
Docker should be installed as pre-requisite. You can install it from https://www.docker.com/get-started

I've followed https://dev.to/xandecodes/spinning-up-postgresql-with-docker-compose-dashboard-g1m

This link says version as latest. But When version is 18+ this setup does not work.

So, update the version to `postgres:17.9`. Now, it also gives me postgres web running at `http://localhot:8081`

